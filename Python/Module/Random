Random Module
 - Random 모듈은 다양한 분포에 대한 의사 난수 생성기를 구현한다.

● random.random()
   - 0 이상 1 미만의 구간에서 부동 소수점 숫자를 반환한다.

● random.choices(population, k=n)
   - population 범위 안의 숫자를 무작위로 선택하여 길이가 n인 리스트를 반환한다.

● random.choices(population, weight, k=n)
   - population 범위 안의 숫자를, weight에 의해 상대 가중치를 두고 선택하여 길이가 n인 리스트를 반환한다.
   
◎ 리스트 [1, 2, 3, 4]의 요소에 각각 2:1:4:3의 비중을 두고 선택하여 길이기 2인 리스트를 반환하는 경우
  return random.choices([1, 2, 3, 4], [2, 1, 4, 3], k=2)
