Math Module
 - Math 모듈은 수학 함수에 대한 접근을 제공한다.

● math.sqrt(x)
   - x의 제곱근을 반환한다.

● math.pow(x,y)
   - x의 y제곱을 반환한다.
   
● math.floor(x)
   - x를 내림한 값을 반환한다.
   
● math.ceil(x)
   - x를 올림한 값을 반환한다.
   
● math.comb(n,k)
   - n개 중에서 k개를 선택하는 경우의 수(nCk)를 반환한다.
