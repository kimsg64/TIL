Built in Object
  - 웹 브라우저에 내장되어 있는 객체이다.

1. window
  - DOM 문서를 담은 창을 의미한다.

2. document
  - 브라우저가 불러온 웹 페이지를 의미한다.
  - write(markup): document에 markup을 포함한 텍스트를 적는다.

3. location
  - 객체가 연결된 장소(URL)를 의미한다.

4. history
  - 브라우저의 세션 기록, 즉 현재 페이지를 불러온 탭, 프레임의 방문 기록을 조작하는 메서드를 제공한다.

5. screen
  - 현재 창이 렌더링되는 스크린을 의미한다.
