Element.id
 - HTML 요소의 id 속성을 반영한다.
Element.removeAttribute()
 - HTML 요소의 속성을 제거한다.

◎ #today 속성을 추가할 때
  document.querySelector('.dates').id = today

◎ #today 속성을 제거할 때
  document.querySelector('.dates').removeAttribute('id')
