Sequence
  - 시퀀스는 유일한 값인 일련번호를 생성해 주는 오라클 객체이다.
  - 한 번 생성된 값은 더 이상 생성되지 않으며, 순차적으로 증가하는 값을 생성해 준다.
  - create sequence 명령으로 생성할 수 있다.
  - start with n 명령을 추가하면 n부터 시퀀스를 시작할 수 있다.
  - increment by n 명령을 추가하면 시퀀스의 간격을 n으로 설정할 수 있다.
  - select * from user_sequences 명령으로 현재 존재하는 시퀀스 객체를 확인할 수 있다.

● sequence.nextval
  - 현재 시퀀스의 다음 값을 반환한다.

● sequence.currval
  - 현재 시퀀스의 값을 반환한다.
