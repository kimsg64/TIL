View
  - join문의 결과, 즉, 복수의 테이블에서 나온 field들을 섞어서 만든 가상의 테이블로, 실제 데이터가 생성되지는 않는다.
  - create view view_name as subquery 형태로 view를 생성할 수 있으며, 같은 이름의 view가 이미 존재한다면 새 테이블이 생성되지 않는다.
  - create or replace view view_name as subquery 형태로도 view를 생성할 수 있는데, 같은 이름의 view가 존재하는 경우 이를 대체한다.
  - view 역시 일반 테이블처럼 데이터의 추가, 수정, 삭제 등이 가능하다.
  - drop view view_name으로 view를 삭제할 수 있다.
