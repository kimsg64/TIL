InetAddress
  - 컴퓨터 사이의 통신을 할 때 정보를 받는 컴퓨터의 IP주소가 필요하므로, 자바를 통해 통신을 하기 위해서는 상대방 컴퓨터의 IP주소를 객체로 만들어야 한다.
  - InetAddress를 통해 IP주소를 객체로 만들 수 있다.
  
  
● getLocalHost()
  - Local Host(현재 컴퓨터)의 IP 주소로 InetAddress 객체를 생성한다.

● getByName(URL)
  - 전달된 웹 서버의 URL 정보로 IP 주소가 담긴 InetAddress 객체를 생성한다.

● getAllByName()
  - 전달된 웹 서버의 URL 정보로 IP 주소가 모두 담긴 InetAddress 객체를 생성한다.

● getHostAddress()
  - InetAddress 객체로부터 IP 주소를 가져온다.

● getHostName()
  - InetAddress 객체로부터 도메인을 가져온다.
