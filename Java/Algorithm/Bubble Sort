Bubble Sort(오름차순)
  - 연속되는 두 데이터를 비교하여, 더 작은 데이터를 앞으로 보낸다.
  
◎ a, b, c, d의 대소관계가 b > d > c > a 일 때, array = [a, b, c, d] 배열을 오름차순으로 정렬하는 경우
  - array[0], array[1]을 비교
    a < b이므로 그대로 유지 (array = [a, b, c, d])
  - array[1], array[2]를 비교
    b > c이므로 temp를 만들어 b와 c를 swap (array = [a, c, b, d])
  - array[2], array[3]을 비교
    b > d이므로 temp를 만들어 b와 d를 swap (array = [a, c, d, b])
