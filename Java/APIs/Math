Math
  - 기본적인 수학 공식들을 제공하는 클래스이다.
  - java.lang 패키지에 내장되어 있어 import 없이 사용할 수 있다.
  
● random()
  - 0.0 이상 1.0 미만의 범위에서 무작위 수(난수)를 반환한다.
  
◎ n 이상 m 미만의 범위에서 무작위 수를 반환하는 경우
  double ranDouble = Math.random();
  int ranInt = (int)(ranDouble*(m-n)+n);
