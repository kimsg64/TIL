RandomAccessFile
  - 외부 파일에 접근할 수 있는 클래스이다.
  - 생성자 함수의 첫 번째 매개변수로 파일 객체를 전달하고, 두 번째 매개변수로 w(write only), r(read only), rw(read-write) 옵션을 전달한다.

  ● writeBytes(String): 해당 객체에 전달된 문자열을 쓴다.
  
  ● writeInt(int): 해당 객체에 전달된 숫자를 쓴다.
  
  ● seek(index): 해당 객체의 index로 포인터를 이동한다.
  
