● Java Developer Kit
 - 컴파일러(javac.exe)를 통해 소스코드(.java)를 기계어(Binary code)로 변환하면 클래스(.class)가 생성되는데, 이를 Java.exe로 실행할 수 있다.

● 환경변수 설정
 - OS가 자동으로 해당 프로그램을 찾을 수 있도록 경로를 설정하는 것
 - 사용자 변수는 현재 로그인된 사용자에 대해서만 설정하는 것이고, 시스템 변수는 모든 사용자에 대해서 설정하는 것이다.
 
