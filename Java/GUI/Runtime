Runtime
  - 외부 파일에 대한 정보를 담고 있는 Runtime 객체를 생성할 수 있다.
  ● getRuntime(): Runtime 인스턴스를 생성한다.
  ● exec("url"): 전달받은 url에 해당하는 외부 파일을 실행한다. 해당 외부 파일이 웹 브라우저인 경우, url에 한 칸 공백을 두고 주소를 전달하면 실행 시에 해당 주소로 이동한다.
